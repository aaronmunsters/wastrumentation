(aspect
    (global >>>GUEST>>>
        console.log("Hello from the start!");
    <<<GUEST<<<)

    (advice apply (func    WasmFunction)
                  (args    MutDynArgs)
                  (results MutDynResults) >>>GUEST>>>

        // pre apply
        console.log(`Passed argument: ${args.get<i32>(0)}`);
        func.apply();
        console.log(`Passed argument: ${args.get<i32>(0)}, result: ${results.get<i32>(0)}`);

        // post apply
        
        

    <<<GUEST<<<))